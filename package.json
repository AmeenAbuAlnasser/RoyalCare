{
  "scripts": {
    "cleanup:phantom-invoices": "node scripts/cleanup-phantom-invoices.mjs",
    "cleanup:phantom-invoices:apply": "node scripts/cleanup-phantom-invoices.mjs --apply"
  },
  "devDependencies": {
    "ts-node": "^10.9.2",
    "tsconfig-paths": "^4.2.0"
  }
}
